import { ExploreImageDirective } from './explore-image.directive';

describe('ExploreImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ExploreImageDirective();
    expect(directive).toBeTruthy();
  });
});
